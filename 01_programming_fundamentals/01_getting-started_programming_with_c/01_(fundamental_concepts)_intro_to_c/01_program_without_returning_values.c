/*
 * Program Title: Simple Program Without Returning Values
 * Author: Achly
 * Date: June 2023
 * Description: This program prints a simple message to the console without returning any values from the `main` function.
 *
 * Logic: The program uses `printf` to display a message on the console.
 * Preconditions: None
 * Postconditions: A message is displayed on the console.
 *
 * Program Flow:
 * 1. The program starts with the `main` function.
 * 2. The `printf` function is used to print the message "Hello Mom!" to the console.
 * 3. The program ends after the message is displayed.
 */

#include <stdio.h>

int main(void) {
    // Print a simple message to the console
    printf("Hello Mom!"); // Display the message
    return 0; // Indicate successful completion
}
